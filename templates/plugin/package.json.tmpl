{
  "name": "__SLUG__",
  "version": "__VERSION__",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "bun run scripts/build.ts",
    "dev": "bun run scripts/dev.ts",
    "block:new": "bun run scripts/scaffold.ts",
    "lint": "bun run lint:types && bun run lint:css && bun run lint:iframe",
    "lint:types": "tsc --noEmit",
    "lint:css": "stylelint 'blocks/**/src/**/*.css'",
    "lint:iframe": "bun run scripts/lint-iframe.ts",
    "test": "bun test",
    "check": "bun run lint && bun test",
    "zip": "bun run scripts/zip.ts"
  },
  "devDependencies": {
    "@types/bun": "^1.2.0",
    "browserslist": "^4.28.7",
    "lightningcss": "^1.33.0",
    "stylelint": "^17.14.1",
    "stylelint-config-standard": "^40.0.0",
    "typescript": "^5.9.0"
  }
}